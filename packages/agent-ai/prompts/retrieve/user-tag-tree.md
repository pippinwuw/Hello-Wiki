## 已选定知识域

`{{selectedDomain}}`

## 标签树（PostgreSQL / 后端接口）

{{tagTree}}

## 主 Agent 建议的检索分解（请结合标签树修订 targetTags）

{{suggestedSearchPlan}}

---

{{analysisHint}}

请根据标签树为 `nextSearchQueries` 填写合适的 `targetTags`（使用树内路径，如 `functional_area.registration`，不要加 domain 前缀）。修订完成后将执行 Python 混合检索。
