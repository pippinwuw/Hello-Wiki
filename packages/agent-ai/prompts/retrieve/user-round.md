## 第 {{roundIndex}} 轮检索结果

本轮执行的子问题：

{{executedPlan}}

召回候选：

{{hits}}

{{degradedLine}}

---

{{analysisHint}}

请结合**此前全部对话**（含任务参数与标签树）分析本轮结果，更新 `relevantPageIds`、判断是否 `sufficient`，并给出后续 `nextSearchQueries`（若已充分则置空）。输出完整 JSON。
